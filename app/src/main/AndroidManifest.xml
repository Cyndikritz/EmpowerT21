<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.will.downsyndromeui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UploadGalleryImage"></activity>

        <activity
            android:name=".Gallery"
            android:label="@string/title_activity_gallery" />
        <activity android:name=".Sentences" />
        <activity android:name=".NumberFlashcard" />
        <activity android:name=".Settings" />
        <activity android:name=".FlashCard" />
        <activity android:name=".Dashboard" />
        <activity android:name=".Register" />
        <activity android:name="Admin.MainActivityAdmin" />
        <activity android:name="Admin.ImagesforAdmin" />
        <activity android:name="Admin.images" />
        <activity android:name="Admin.questions1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>